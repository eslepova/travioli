import * as React from 'react';
import { Layout } from './Layout';

/**
 * Hotel list page
 */
export const SearchResultScreen = () => {
  return (
    <Layout>
        Hotels list
    </Layout>
  );
}
